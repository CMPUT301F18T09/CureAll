class Map {
	Public locationset = None
	Public type = None
	if type = 1://show the map to select a specific location 
	if type = 2://show the map of all Records
	//show the map, if locationset != None, show the Mark Place, else show the normal map
}