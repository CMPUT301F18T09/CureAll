class AddProblem {
	ArrayList<Problems> problemList = new ArrayList<Problems>()//this list use to save the list of problem which read from the file, then append user's enter at the end of list, the store it to file later
	
	public ReadFromFIle(problemList = gson.fromJson(....))

	public SaveToFile(){
		problemList.add(new(Problems(String Title, String descrption)))
		save(....)
	}
	
}
