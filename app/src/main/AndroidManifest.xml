<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cmput301f18t09.cureall">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.PatientSignUpActivity" />
        <activity android:name=".Activities.ProviderSignUpActivity" />
        <activity android:name=".Activities.UserLoginActivity" />
        <activity android:name=".Activities.ProviderMainPageActivity" />
        <activity android:name=".Activities.ProviderAListOfProblemsPageActivity" />
        <activity android:name=".Activities.ProviderCommentPageActivity" />
        <activity android:name=".Activities.ProviderProblemDetailPageActivity" />
        <activity android:name=".Activities.ProviderRecordDetailPageActivity"></activity>
    </application>

</manifest>