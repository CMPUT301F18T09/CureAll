import java.util.*;

class Animating {
	public static Problems problem;
	private ArrayList<Records> record = new ArrayList<Records>();
	SplitRecords();//split each record, so we can use getphoto() to get the photos
	Animating();
}